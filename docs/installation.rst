============
Installation
============

At the command line::

    $ easy_install guacamole 

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv guacamole 
    $ pip install guacamole 
